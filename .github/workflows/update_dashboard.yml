name: Update Training Dashboard

on:
  schedule:
    - cron: '0 23 * * *'   # 23:00 UTC = midnight Munich (CET)
  workflow_dispatch:         # manual trigger button in GitHub UI

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install garminconnect python-dotenv fitparse numpy

      - name: Download and parse new activities
        env:
          GARMIN_EMAIL:    ${{ secrets.GARMIN_EMAIL }}
          GARMIN_PASSWORD: ${{ secrets.GARMIN_PASSWORD }}
        run: python3 garmin_download.py

      - name: Fetch intervals.icu data
        env:
          ICU_API_KEY:    ${{ secrets.ICU_API_KEY }}
          ICU_ATHLETE_ID: ${{ secrets.ICU_ATHLETE_ID }}
        run: python3 fetch_icu_data.py

      - name: Rebuild all data
        run: |
          chmod +x rebuild_all.sh
          ./rebuild_all.sh --from 1

      - name: Commit and push if data changed
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add fit_files/*.json dashboard_data.json running_cloud.json cycling_cloud.json cycling_curve.json running_bests.json || true
          git diff --cached --quiet && echo "No changes to commit" || \
            git commit -m "Dashboard update $(date -u '+%Y-%m-%d %H:%M UTC')" && git push
